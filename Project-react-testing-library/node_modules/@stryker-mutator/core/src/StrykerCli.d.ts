import * as commander from 'commander';
import { PartialStrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
export default class StrykerCli {
    private readonly argv;
    private readonly program;
    private readonly runMutationTest;
    private readonly log;
    private command;
    private strykerConfig;
    constructor(argv: string[], program?: commander.Command, runMutationTest?: (options: PartialStrykerOptions) => Promise<import("@stryker-mutator/api/src/report/MutantResult").default[]>, log?: Logger);
    run(): void;
}
//# sourceMappingURL=StrykerCli.d.ts.map